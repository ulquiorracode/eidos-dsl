// src/Eidos/Resolver/EidosResolver.cs
using System.Collections.Immutable;
using Eidos.Abstractions;
using Eidos.Models;

namespace Eidos.Resolver
{
    /// <summary>
    /// Default sealed resolver implementation.
    /// </summary>
    public sealed class EidosResolver : ResolverBase
    {
        // Uses base behavior; override if you need custom policies (recursion depth, wildcard semantics, etc).
    }
}

